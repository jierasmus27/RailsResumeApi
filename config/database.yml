# SQLite version 3.x
#   gem install sqlite3
#
#   Ensure the SQLite 3 gem is defined in your Gemfile
#   gem 'sqlite3'
#
default: &default
  timeout: 5000
  host: localhost
  adapter: postgresql
  encoding: unicode
  pool: 5
  username: rails_resume
  password: s0m3th1ng

development:
  <<: *default
  database: rails_resume_api_development

# Warning: The database defined as "test" will be erased and
# re-generated from your development database when you run "rake".
# Do not set this db to the same as development or production.
test:
  <<: *default
  database: rails_resume_api_test

production:
  <<: *default
  database: rails_resume_api_production
